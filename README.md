# TB-Core

## Deploy

```bash
cp .env.example .env
# adjust env
make up

npm install
npm install --force
npm run production

composer install
composer dump-autoinstall

make install
make optimize
```
